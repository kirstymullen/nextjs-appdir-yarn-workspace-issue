{
  "name": "nextjs-appdir-yarn-workspace-issue",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "lerna run dev --parallel"
  },
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "lerna": "^5.6.2"
  }
}
